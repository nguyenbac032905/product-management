extends ../../layouts/default.pug
include ../../mixins/box-head.pug
include ../../mixins/product-layout.pug

block main
    if(products.length > 0)
        +product-grid("Kết quả tìm kiếm",products)
    else 
        h1.text-center không tìm thấy sản phẩm nào